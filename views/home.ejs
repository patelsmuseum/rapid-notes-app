<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>homepage </title>
</head>
<body>
    <h1>Home page</h1>

    <% if (locals.user){ %>                    
        <%= user.name %> 
        <%= user.id%>
        <span>(user is available at this page )</span>
        <a href="/auth/LogOut"> LogOut</a>
    <% }else{ %>
        <a href="/auth/signin">Sign In</a>
    <% } %>
    
</body>
</html>